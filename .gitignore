node_modules/
dist/
.expo/
*.tsbuildinfo

# Secrets
.env
.env.local
.env.*.local

# REAL RECEIPTS. Contains names, addresses, partial payment info.
# Never version-controlled. See PROMPT.md §7.
research/corpus/
!research/corpus/.gitkeep

.DS_Store
coverage/

# Exported Claude Code session transcripts. Local working notes — they carry
# full conversation context and are not part of the codebase.
session*.md
